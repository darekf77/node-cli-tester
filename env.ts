import type { EnvOptions } from 'tnp/src';

const env: Partial<EnvOptions> = {
  website: {
    domain: 'node-cli-tester.example.domain.com',
    title: 'Node Cli Tester',
  },
};
export default env;
